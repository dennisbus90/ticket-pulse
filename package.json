{
  "name": "ticket-pulse",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered ticket quality analysis for Jira and Azure DevOps",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:shared": "cd packages/shared && tsc",
    "build:jira:frontend": "cd packages/jira/static/ticket-pulse && npm run build",
    "build:azure": "cd packages/azure-devops && npm run build",
    "build:proxy": "cd packages/azure-function && tsc",
    "dev:jira": "cd packages/jira/static/ticket-pulse && npm run dev",
    "dev:azure": "cd packages/azure-devops && npm run dev",
    "dev:azure:ado": "cd packages/azure-devops && npm run dev:ado",
    "deploy:jira": "cd packages/jira && forge deploy && forge install --upgrade",
    "package:azure": "cd packages/azure-devops && npx tfx-cli extension create --manifest-globs vss-extension.json",
    "lint": "npm run lint --workspaces --if-present"
  }
}
