{
  "name": "@ticket-pulse/jira",
  "version": "1.0.0",
  "private": true,
  "description": "Ticket Pulse — Jira Forge extension for ticket quality analysis",
  "scripts": {
    "build:frontend": "cd static/ticket-pulse && npm run build",
    "install:frontend": "cd static/ticket-pulse && npm install",
    "dev": "cd static/ticket-pulse && npm run dev",
    "lint": "tsc --noEmit",
    "deploy": "cd static/ticket-pulse/ && npm run build && cd ../.. && forge deploy && forge install --upgrade",
    "deploy:prod": "cd static/ticket-pulse/ && npm run build && cd ../.. && forge deploy -e production && forge install --upgrade"
  },
  "dependencies": {
    "@forge/api": "^4.3.0",
    "@forge/resolver": "^1.7.1",
    "@ticket-pulse/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
